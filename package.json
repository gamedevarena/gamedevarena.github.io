{
  "name": "game-loop-arena",
  "version": "1.0.0",
  "description": "Game Dev Arena with live reload server",
  "main": "server.js",
  "scripts": {
    "build": "node scripts/build-events.js",
    "build:check": "node scripts/build-events.js --check",
    "start": "node ./server/server.js",
    "dev": "npm run build && nodemon ./server/server.js"
  },
  "dependencies": {
    "chokidar": "^3.5.3",
    "express": "^4.18.2",
    "ws": "^8.13.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.3"
  }
}
